#!/usr/bin/env bash
# Agent-only convenience wrapper around `tools/win` for Windows git.
#
# Usage:
# - `bash tools/gitw status`
# - `bash tools/gitw push origin main`
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec bash "$script_dir/win" git "$@"
